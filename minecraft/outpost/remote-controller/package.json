{
  "name": "@outpost/remote-controller",
  "version": "2026.1.0",
  "description": "Remote controller for Outpost",
  "scripts": {
    "generate-token": "mkdir -p credential && openssl rand -hex 32 > credential/token",
    "start": "bun start-service.ts"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "@types/bun": "^1.3.14",
    "prettier-plugin-jsdoc": "^1.8.1"
  }
}
